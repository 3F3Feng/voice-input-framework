# Voice Input Framework - STT Service Dependencies
# 运行在 vif-stt conda 环境中

# Web 框架
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6
pydantic>=2.5.0

# HTTP 客户端 (调用 LLM 服务)
httpx>=0.26.0

# 音频处理
# numpy 锁定 1.x:numpy 2.x 的 C-API 破坏性变更会与旧编译的二进制
# (如 conda 的 scipy、为 numpy 1.x 编译的模块)冲突,报 _ARRAY_API not found
numpy>=1.26.4,<2.0

# MLX 加速 (仅 Apple Silicon;非 Apple 机器可跳过,自动回落 whisper_turbo)
# 注意:mlx-audio 强制 transformers>=5.0;whisper_turbo 的 pipeline API 在 5.x 兼容
mlx>=0.31.0
mlx-audio>=0.4.2

# STT 模型
# - MLX 引擎(qwen_asr_mlx_native / whisper_mlx)经 mlx-audio,transformers 5.x
# - whisper_turbo 引擎用 transformers pipeline(4.x/5.x 均兼容)
transformers>=4.37.0
torch>=2.1.0
torchaudio>=2.1.0

# 日志
rich>=13.7.0

# WebSocket
websockets>=12.0
