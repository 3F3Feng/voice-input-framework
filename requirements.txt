# ⚠️ 已废弃,不要再用。依赖以 pyproject.toml 为准,建环境请用 scripts/setup-env.sh。
#    这份清单选不了 CUDA / ROCm 版 PyTorch;它无条件装 mlx,Linux 上装得上、一 import 就报 libmlx.so 找不到。
#    只为老脚本兼容保留。

# Voice Input Framework - Server Dependencies (单体架构)
# 安装: python3 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt

# ============== Web 框架 ==============
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
websockets>=12.0
httpx>=0.26.0
python-multipart>=0.0.6
pydantic>=2.5.0

# ============== 音频处理 ==============
numpy>=1.26.0
scipy>=1.12.0
noisereduce>=3.0.0

# ============== STT 模型依赖 ==============
# --- Whisper (transformers) ---
transformers>=4.37.0
torch>=2.1.0
torchaudio>=2.1.0


# --- MLX 加速 (Apple Silicon) ---
mlx>=0.31.0
mlx-lm>=0.31.0
mlx-audio>=0.4.2

# --- whisper.cpp (可选，需要编译) ---
# 无 Python 依赖，使用 subprocess 调用

# ============== 日志和工具 ==============
rich>=13.7.0

# ============== 开发/测试 ==============
pytest>=7.4.0
pytest-asyncio>=0.21.0
ruff>=0.1.0
