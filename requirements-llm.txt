# ⚠️ 已废弃,不要再用。依赖以 pyproject.toml 为准,建环境请用 scripts/setup-env.sh。
#    这份清单选不了 CUDA / ROCm 版 PyTorch;MLX 相关依赖只在 Apple Silicon 上能用。
#    只为老脚本兼容保留。

# Voice Input Framework - LLM Service Dependencies
# 运行在 mlx-test conda 环境中

# Web 框架
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0

# MLX LLM (Apple Silicon)
mlx>=0.31.0
mlx-lm>=0.31.0

# 日志
rich>=13.7.0
