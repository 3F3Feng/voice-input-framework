# Voice Input Framework - LLM Dependencies (NVIDIA CUDA)
# For Windows/Linux with NVIDIA GPU
# Install: pip install -r requirements/base.txt -r requirements/llm-cuda.txt
#
# PyTorch with CUDA (install separately):
#   pip install torch --index-url https://download.pytorch.org/whl/cu124
#
# For Qwen3.5 models, transformers from PyPI may not support the architecture yet:
#   pip install git+https://github.com/huggingface/transformers.git

# ============== LLM Models ==============
transformers>=4.37.0
accelerate>=0.28.0

# ============== Quantization ==============
bitsandbytes>=0.43.0

# ============== CUDA Optimization (optional) ==============
# flash-attn>=2.6.0      # Flash Attention 2 (requires compilation on Windows)

# ============== Qwen3.5 MLA Attention Acceleration ==============
# Qwen3.5 uses Multi-head Latent Attention (MLA). Without these,
# the model falls back to slow PyTorch implementation (~27s per request):
#   The fast path is not available... Falling back to torch implementation.
#
# Install (may need compilation tools on Windows):
#   pip install flash-linear-attention causal-conv1d
