# Voice Input Framework - STT Dependencies (NVIDIA CUDA)
# For Windows/Linux with NVIDIA GPU
# Install: pip install -r requirements/base.txt -r requirements/stt-cuda.txt
#
# PyTorch with CUDA (install separately):
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu124

# ============== STT Models ==============
qwen_asr>=0.0.6
transformers>=4.37.0,<5.0

# ============== CUDA Optimization (optional) ==============
# flash-attn>=2.6.0    # ~15% STT inference speedup (requires compilation on Windows)
