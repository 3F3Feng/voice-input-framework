# Voice Input Framework - STT Service Dependencies (Windows CUDA)
# 运行在 vif-stt conda 环境中 (4090 优化)

# ============== Web 框架 ==============
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6
pydantic>=2.5.0

# ============== HTTP 客户端 (调用 LLM 服务) ==============
httpx>=0.26.0

# ============== 音频处理 ==============
numpy>=1.26.0

# ============== STT 模型 ==============
qwen_asr>=0.0.6
transformers>=4.37.0,<5.0

# ============== CUDA (PyTorch via index-url) ==============
# pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu124

# ============== CUDA 优化 (可选) ==============
# flash-attn>=2.6.0    # Flash Attention 2 (需编译)
# bitsandbytes>=0.43.0 # int8 量化

# ============== 日志 ==============
rich>=13.7.0

# ============== WebSocket ==============
websockets>=12.0
